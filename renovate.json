{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "schedule": [
    "after day 5 and before day 28"
  ],
  "timezone": "UTC",
  "customManagers": [
    {
      "customType": "regex",
      "description": "Dockerfile ARG pins preceded by # renovate: (datasource + depName + optional versioning)",
      "managerFilePatterns": [
        "/(^|/|\\.)([Dd]ocker|[Cc]ontainer)file$/",
        "/(^|/)([Dd]ocker|[Cc]ontainer)file[^/]*$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>\\S+)(?: registryUrl=(?<registryUrl>\\S+))?(?: versioning=(?<versioning>\\S+))?\\s*(?:\\r\\n|\\n|\\r)ARG [A-Z0-9_]+=(?<currentValue>[^\\r\\n]+)"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}"
    },
    {
      "customType": "regex",
      "description": "Gradle `def fireflyTag = '<tag>'` style pins preceded by // renovate: (used in cadc-sso/lib/build.gradle to track the Caltech-IPAC/firefly tag whose firefly.jar is consumed as a compile dep)",
      "managerFilePatterns": [
        "/(^|/)build\\.gradle$/"
      ],
      "matchStrings": [
        "// renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>\\S+)(?: registryUrl=(?<registryUrl>\\S+))?(?: versioning=(?<versioning>\\S+))?\\s*(?:\\r\\n|\\n|\\r)def [A-Za-z0-9_]+ = '(?<currentValue>[^']+)'"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}loose{{/if}}"
    }
  ],
  "packageRules": [
    {
      "description": "PyPI versions in Dockerfiles follow PEP 440 (comments omit explicit versioning=)",
      "matchDatasources": [
        "pypi"
      ],
      "matchManagers": [
        "custom.regex"
      ],
      "versioning": "pep440"
    },
    {
      "description": "deb datasource in Dockerfiles (e.g. CARTA via PPA) uses native deb versioning: epoch colons, ~prerelease suffixes, -N revisions",
      "matchDatasources": [
        "deb"
      ],
      "matchManagers": [
        "custom.regex"
      ],
      "versioning": "deb"
    }
  ]
}
